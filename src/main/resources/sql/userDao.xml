<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.keyboard.repository.AuthDao">
    <!-- 로그인 인증 -->
    <select id="findByLoginId" parameterType="String" resultType="memberEntity">
        select * from member where login_id = #{loginId}
    </select>

    <!-- 중복확인 -->
    <select id="existsById" parameterType="String" resultType="String">
        select login_id from member where login_id = #{loginId}
    </select>

    <!--  회원가입  -->
    <insert id="join" parameterType="memberEntity">
        INSERT INTO member(login_id, password, user_name, phone_num, address, email, birthday)
        VALUES(#{loginId}, #{password}, #{userName}, #{phoneNum},#{address},#{email}, #{birthday})
    </insert>
</mapper>
